import { GreaterThenFilterPipe } from './greater-then-filter.pipe';

describe('GreaterThenFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GreaterThenFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
