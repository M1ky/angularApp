import { LesserThenFilterPipe } from './lesser-then-filter.pipe';

describe('LesserThenFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LesserThenFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
